export default function PlaceholderModule({ title }) {
  return (
    <div className="card">
      <h2>{title}</h2>
      <p className="muted">This module isn't wired up yet in this build.</p>
    </div>
  );
}
